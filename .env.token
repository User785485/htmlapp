# Ajoutez votre nouveau token GitHub (ne partagez pas celui-ci dans la conversation)
# GITHUB_TOKEN=votre_nouveau_token

# Après avoir remplacé la ligne ci-dessus avec votre vrai token,
# exécutez: type .env.token >> .env.local
